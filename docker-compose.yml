services:
  postgres:
    image: postgres:16
    container_name: de-postgres
    environment:
      POSTGRES_PASSWORD: devpassword
      POSTGRES_DB: de_upskilling
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: